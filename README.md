# Online C++ Compiler

A full stack online C++ compiler built with:

- **Frontend** — React + Vite + Tailwind CSS
- **Backend** — Node.js + Express
- **Compiler** — g++ (MinGW)

## How to run

### Backend
cd backend
npm install
npx nodemon index.js


### Frontend
cd frontend
npm install
npm run dev
